/**
 * Shared API contracts: request/response types, enums, constants.
 * Feature packages import from `@pos/contracts` — never duplicate DTOs.
 */
export type {};

export { PermissionCodes, type PermissionCode } from "./rbac/permission-codes.js";
