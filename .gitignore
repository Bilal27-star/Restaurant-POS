# -----------------------------------------------------------------------------
# Dependencies & package managers
# -----------------------------------------------------------------------------
node_modules/

# -----------------------------------------------------------------------------
# Build outputs & caches
# -----------------------------------------------------------------------------
dist/
build/
target/
.next/
.turbo/
.cache/
coverage/

# -----------------------------------------------------------------------------
# Local / secrets (never commit)
# -----------------------------------------------------------------------------
.env
.env.local
.env.*.local

# -----------------------------------------------------------------------------
# OS & editor noise
# -----------------------------------------------------------------------------
.DS_Store
*.log

# -----------------------------------------------------------------------------
# Installers & desktop bundles (generated locally / in CI)
# -----------------------------------------------------------------------------
*.dmg
*.exe
*.msi
*.app

# -----------------------------------------------------------------------------
# Tauri — Rust build tree + embedded desktop payloads (rebuilt by scripts/CI)
# -----------------------------------------------------------------------------
apps/tauri-pos/src-tauri/target/
apps/tauri-pos/src-tauri/resources/bundled-api/
apps/tauri-pos/src-tauri/resources/node-runtime/

# -----------------------------------------------------------------------------
# Local embedded PostgreSQL data (dev API — apps/api)
# -----------------------------------------------------------------------------
apps/api/.embedded-pg-dev/
