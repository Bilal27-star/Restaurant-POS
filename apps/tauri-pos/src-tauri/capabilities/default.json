{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Production POS: core UI, desktop IPC bridge, logs, shell open, dialogs, autostart.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-pos-desktop",
    "log:default",
    "shell:default",
    "dialog:default",
    "autostart:default"
  ]
}
